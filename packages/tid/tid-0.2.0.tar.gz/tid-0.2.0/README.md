# tid

Provides an interface for getting the LWP id as visible in `ps`, `top`,
and other utilities.

```python
import tid


tid.gettid()
```

# development

```
poetry install
poetry build
poetry publish
```
