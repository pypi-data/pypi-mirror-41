{
    "name": "phenotypes",
    "entities": [
        {
            "name": "session",
            "pattern": "ses-([a-zA-Z0-9]+)",
            "mandatory": false,
            "directory": "{{root}}/{session}",
            "missing_value": "nc"
        }
    ]
}
